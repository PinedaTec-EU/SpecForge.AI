{
  "name": "specforge-ai",
  "displayName": "SpecForge.AI",
  "description": "Minimal VS Code extension for exploring SpecForge.AI user stories.",
  "version": "0.0.1",
  "publisher": "local",
  "license": "MIT",
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:specForge.userStories",
    "onView:specForge.auditStream",
    "onCommand:specForge.refreshUserStories",
    "onCommand:specForge.createUserStory",
    "onCommand:specForge.importUserStory",
    "onCommand:specForge.initializeRepoPrompts",
    "onCommand:specForge.openPromptTemplates",
    "onCommand:specForge.openWorkflowView",
    "onCommand:specForge.openCliWorkflowPortal",
    "onCommand:specForge.openExecutionSettings",
    "onCommand:specForge.openMainArtifact",
    "onCommand:specForge.continuePhase",
    "onCommand:specForge.approveCurrentPhase",
    "onCommand:specForge.requestRegression",
    "onCommand:specForge.restartUserStoryFromSource",
    "onCommand:specForge.showUserStoryDetails",
    "onCommand:specForge.showOutput"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "specForge",
          "title": "SpecForge.AI",
          "icon": "media/specforge.svg"
        }
      ],
      "panel": [
        {
          "id": "specForgePanel",
          "title": "SpecForge.AI",
          "icon": "media/specforge.svg"
        }
      ]
    },
    "views": {
      "specForge": [
        {
          "id": "specForge.userStories",
          "name": "User Stories",
          "type": "webview"
        }
      ],
      "specForgePanel": [
        {
          "id": "specForge.auditStream",
          "name": "Audit Stream",
          "type": "webview"
        }
      ]
    },
    "mcpServerDefinitionProviders": [
      {
        "id": "specForge.workspaceMcp",
        "label": "SpecForge Workspace MCP"
      }
    ],
    "commands": [
      {
        "command": "specForge.refreshUserStories",
        "title": "Refresh User Stories"
      },
      {
        "command": "specForge.createUserStory",
        "title": "Create User Story"
      },
      {
        "command": "specForge.importUserStory",
        "title": "Import User Story"
      },
      {
        "command": "specForge.initializeRepoPrompts",
        "title": "Export All Prompt Templates"
      },
      {
        "command": "specForge.openPromptTemplates",
        "title": "Customize Prompt Templates"
      },
      {
        "command": "specForge.openWorkflowView",
        "title": "Open Workflow View"
      },
      {
        "command": "specForge.openCliWorkflowPortal",
        "title": "Open CLI Workflow Portal"
      },
      {
        "command": "specForge.openExecutionSettings",
        "title": "Open SpecForge Configuration"
      },
      {
        "command": "specForge.openMainArtifact",
        "title": "Open Main Artifact"
      },
      {
        "command": "specForge.continuePhase",
        "title": "Continue Phase"
      },
      {
        "command": "specForge.approveCurrentPhase",
        "title": "Approve Current Phase"
      },
      {
        "command": "specForge.requestRegression",
        "title": "Request Regression"
      },
      {
        "command": "specForge.restartUserStoryFromSource",
        "title": "Restart User Story From Source"
      },
      {
        "command": "specForge.showUserStoryDetails",
        "title": "Show User Story Details"
      },
      {
        "command": "specForge.showOutput",
        "title": "Show SpecForge Output"
      }
    ],
    "configuration": {
      "title": "SpecForge.AI",
      "properties": {
        "specForge.execution.modelProfiles": {
          "type": "array",
          "default": [],
          "description": "Named model profiles. Each profile declares its own provider and capabilities so the workflow can route phases explicitly. `codex`, `claude`, and `copilot` run natively from their local CLI. Use `openai-compatible` for endpoint-based bridge profiles.",
          "items": {
            "type": "object",
            "required": [
              "name"
            ],
            "properties": {
              "name": {
                "type": "string",
                "description": "Stable profile name used by phase assignments."
              },
              "provider": {
                "type": "string",
                "default": "openai-compatible",
                "enum": [
                  "openai-compatible",
                  "codex",
                  "copilot",
                  "claude"
                ],
                "description": "Provider kind used by this profile. `codex`, `claude`, and `copilot` use their native local CLI. `openai-compatible` uses the configured chat/completions endpoint."
              },
              "baseUrl": {
                "type": "string",
                "description": "Base URL for this profile's endpoint. Required for `openai-compatible`. Omit for native CLI providers."
              },
              "apiKey": {
                "type": "string",
                "default": "",
                "description": "API key for this profile's endpoint. Required for remote `openai-compatible` endpoints. Not used by native CLI execution."
              },
              "model": {
                "type": "string",
                "description": "Concrete model identifier exposed by this profile's endpoint. Optional for native CLI execution, which can fall back to the local provider default."
              },
              "reasoningEffort": {
                "type": "string",
                "enum": [
                  "none",
                  "minimal",
                  "low",
                  "medium",
                  "high",
                  "xhigh"
                ],
                "description": "Optional reasoning effort override. Sent to `openai-compatible` HTTP profiles and Codex CLI profiles when supported."
              },
              "repositoryAccess": {
                "type": "string",
                "default": "none",
                "enum": [
                  "none",
                  "read",
                  "read-write"
                ],
                "description": "Repository access granted to this profile. Refinement, spec, and technical design require read. Implementation and review require read-write."
              }
            }
          }
        },
        "specForge.execution.agentProfiles": {
          "type": "array",
          "default": [],
          "description": "Named agent profiles. Agents define behavior, instructions, repository access, and the model profile they use.",
          "items": {
            "type": "object",
            "required": [
              "name",
              "modelProfile"
            ],
            "properties": {
              "name": {
                "type": "string",
                "description": "Stable agent name used by phase assignments."
              },
              "role": {
                "type": "string",
                "description": "Operational role, such as planner, implementer, reviewer, or release-preparer."
              },
              "modelProfile": {
                "type": "string",
                "description": "Name of the model profile this agent runs on."
              },
              "instructions": {
                "type": "string",
                "description": "Additional behavior instructions injected into the effective phase prompt."
              },
              "repositoryAccess": {
                "type": "string",
                "default": "none",
                "enum": [
                  "none",
                  "read",
                  "read-write"
                ],
                "description": "Repository access granted to this agent."
              },
              "reasoningEffort": {
                "type": "string",
                "enum": [
                  "none",
                  "minimal",
                  "low",
                  "medium",
                  "high",
                  "xhigh"
                ],
                "description": "Optional reasoning effort override for this agent."
              }
            }
          }
        },
        "specForge.execution.phaseAgents": {
          "type": "object",
          "default": {},
          "description": "Assign named agent profiles to workflow phases. Any unassigned phase falls back to the default agent.",
          "properties": {
            "defaultAgent": {
              "type": "string",
              "default": "",
              "description": "Fallback agent used when a specific phase does not declare its own agent."
            },
            "captureAgent": {
              "type": "string",
              "default": "",
              "description": "Optional agent override for capture."
            },
            "refinementAgent": {
              "type": "string",
              "default": "",
              "description": "Optional agent override for refinement."
            },
            "specAgent": {
              "type": "string",
              "default": "",
              "description": "Optional agent override for spec."
            },
            "technicalDesignAgent": {
              "type": "string",
              "default": "",
              "description": "Optional agent override for technical design."
            },
            "implementationAgent": {
              "type": "string",
              "default": "",
              "description": "Optional agent override for implementation."
            },
            "reviewAgent": {
              "type": "string",
              "default": "",
              "description": "Optional agent override for review."
            },
            "releaseApprovalAgent": {
              "type": "string",
              "default": "",
              "description": "Optional agent override for release approval."
            },
            "prPreparationAgent": {
              "type": "string",
              "default": "",
              "description": "Optional agent override for PR preparation."
            }
          }
        },
        "specForge.execution.refinementTolerance": {
          "type": "string",
          "default": "balanced",
          "enum": [
            "strict",
            "balanced",
            "inferential"
          ],
          "description": "How demanding refinement should be before the workflow advances to spec. This also adjusts the temperature sent for the refinement phase."
        },
        "specForge.execution.mvpRigor": {
          "type": "string",
          "default": "medium",
          "enum": [
            "low",
            "medium",
            "high"
          ],
          "description": "How much product detail refinement requires before a user story can become a buildable MVP slice. Low is lean, medium is professional, high is exacting."
        },
        "specForge.execution.reviewTolerance": {
          "type": "string",
          "default": "balanced",
          "enum": [
            "strict",
            "balanced",
            "inferential"
          ],
          "description": "How demanding the review phase should be when assessing findings and pass/fail readiness. This also adjusts the temperature sent for the review phase."
        },
        "specForge.execution.reviewEvidencePolicy": {
          "type": "string",
          "default": "balanced",
          "enum": [
            "strict",
            "balanced",
            "release",
            "advisory"
          ],
          "description": "How the review phase treats missing automated, static, operational, and deferred validation evidence when deciding pass/fail readiness."
        },
        "specForge.execution.technicalDesignSubagentsEnabled": {
          "type": "boolean",
          "default": false,
          "description": "When enabled, technical design runs specialist subagents before synthesizing the final phase artifact."
        },
        "specForge.execution.reviewSubagentsEnabled": {
          "type": "boolean",
          "default": false,
          "description": "When enabled, review runs specialist subagents before synthesizing the final review verdict artifact."
        },
        "specForge.execution.autoRefinementAnswersProfile": {
          "type": "string",
          "default": "",
          "description": "Named agent used to auto-answer refinement questions before handing spec back to the user."
        },
        "specForge.ui.workflowGraphLayoutMode": {
          "type": "string",
          "default": "vertical",
          "enum": [
            "vertical",
            "horizontal"
          ],
          "description": "Preferred workflow graph layout for this user."
        },
        "specForge.ui.workflowGraphInitialZoomMode": {
          "type": "string",
          "default": "actual-size",
          "enum": [
            "actual-size",
            "fit-width"
          ],
          "enumDescriptions": [
            "Open the workflow graph at 100% and center it in the viewport.",
            "Open the workflow graph scaled to the viewport width."
          ],
          "description": "Preferred initial workflow graph zoom for this user."
        },
        "specForge.ui.userStoryListViewMode": {
          "type": "string",
          "default": "category",
          "enum": [
            "category",
            "phase"
          ],
          "description": "Group the sidebar user story list by category or current workflow phase."
        },
        "specForge.ui.enableWatcher": {
          "type": "boolean",
          "default": true,
          "description": "Refresh the explorer and workflow view automatically when .specs files change."
        },
        "specForge.ui.notifyOnAttention": {
          "type": "boolean",
          "default": true,
          "description": "Show notifications when a user story becomes waiting-user, blocked, or completed."
        },
        "specForge.ui.visualTimelineEnabled": {
          "type": "boolean",
          "default": false,
          "description": "Show the visual workflow timeline dock inside the workflow detail view."
        },
        "specForge.features.enableContextSuggestions": {
          "type": "boolean",
          "default": true,
          "description": "Suggest repository context files during refinement using local heuristics and repo neighborhood rules."
        },
        "specForge.features.requireApprovalBranchAcceptance": {
          "type": "boolean",
          "default": false,
          "description": "Require explicit acceptance of the proposed base branch in the workflow detail before approving spec."
        },
        "specForge.features.autoRefinementAnswersEnabled": {
          "type": "boolean",
          "default": false,
          "description": "When refinement blocks spec, let the selected model try to answer the pending questions once with the full workflow context before handing the phase to the user."
        },
        "specForge.features.phaseSkillUsageReportingEnabled": {
          "type": "boolean",
          "default": true,
          "description": "When enabled, phase model prompts ask the invoked model to report any Codex or repository skills used so the phase detail can show them."
        },
        "specForge.features.autoPlayEnabled": {
          "type": "boolean",
          "default": false,
          "description": "Automatically resume workflow playback after manual actions such as approving spec when the next phase can continue."
        },
        "specForge.features.autoReviewEnabled": {
          "type": "boolean",
          "default": false,
          "description": "Automatically continue from implementation into review after implementation artifacts are generated or updated."
        },
        "specForge.features.maxImplementationReviewCycles": {
          "type": "integer",
          "minimum": 1,
          "default": 5,
          "description": "Maximum implementation attempts allowed in the implementation/review loop before automatic continuation stops."
        },
        "specForge.features.destructiveRewindEnabled": {
          "type": "boolean",
          "default": false,
          "description": "When enabled, rewind and regress delete later derived artifacts and branch metadata. When disabled, both only move the workflow state back."
        },
        "specForge.features.pauseOnFailedReview": {
          "type": "boolean",
          "default": false,
          "description": "When enabled, automatically pause workflow playback on a failed review and show the review warning overlay for developer inspection."
        },
        "specForge.features.reviewLearningEnabled": {
          "type": "boolean",
          "default": true,
          "description": "When enabled, implementation retries after failed review may persist generalized lessons into local skills or prompt guardrails so future user stories avoid the same class of issue."
        },
        "specForge.features.reviewLearningSkillPath": {
          "type": "string",
          "default": ".codex/skills/sdd-phase-agents/SKILL.md",
          "description": "Workspace-relative skill path used for local SDD review-learning guardrails when review learning is enabled."
        },
        "specForge.features.completedUsLockOnCompleted": {
          "type": "boolean",
          "default": false,
          "description": "When enabled, completed user stories stay locked against direct rewind or artifact modification. Explicit reopen from the completed phase remains available."
        }
      }
    },
    "menus": {
      "view/item/context": [
        {
          "command": "specForge.openCliWorkflowPortal",
          "when": "view == specForge.userStories && viewItem == userStory",
          "group": "navigation@2"
        }
      ]
    }
  },
  "scripts": {
    "compile": "npm run compile:ts && npm run compile:mcp",
    "compile:ts": "node node_modules/typescript/lib/tsc.js -p ./",
    "compile:mcp": "dotnet publish src/SpecForge.McpServer/SpecForge.McpServer.csproj -c Release -o dist/mcp --no-self-contained /p:DebugType=None /p:DebugSymbols=false && dotnet publish src/SpecForge.Runner.Cli/SpecForge.Runner.Cli.csproj -c Release -o dist/mcp --no-self-contained /p:DebugType=None /p:DebugSymbols=false",
    "watch": "node node_modules/typescript/lib/tsc.js -watch -p ./",
    "compile:tests": "node node_modules/typescript/lib/tsc.js -p tsconfig.tests.json",
    "test:ts": "npm run compile:tests && node --test dist-tests/tests-ts/*.test.js",
    "docs:screenshots": "node tools/render-doc-screenshots.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/vscode": "^1.100.0",
    "typescript": "^5.8.3"
  }
}
